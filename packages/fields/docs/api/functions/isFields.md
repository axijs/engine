[**@axi-engine/fields**](../README.md)

***

[@axi-engine/fields](../README.md) / isFields

# Function: isFields()

> **isFields**(`value`): `value is Fields`

Defined in: fields/src/guards.ts:13

Type guard that checks if a value is an instance of the `Fields` class.
It verifies this by checking the static `typeName` property on the instance.

## Parameters

### value

`unknown`

The value to check.

## Returns

`value is Fields`

`true` if the value is a `Fields` instance, otherwise `false`.
